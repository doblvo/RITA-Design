# Libraries

La librería se organiza por intención: tokens → iconos → componentes → patrones → dominio → adaptadores. Esta secuencia permite ampliar el sistema sin duplicar decisiones entre web/PWA y Flutter.
